## About
This is from [Monocrhome](https://dyutibarma.github.io/monochrome/)


```
 docker run --rm -v "$(pwd):/srv/jekyll" -p 4000:4000 jekyll/jekyll:4 sh -c "bundle install && jekyll serve --host 0.0.0.0"      
```